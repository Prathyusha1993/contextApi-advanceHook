import React from "react";
import Parent from "./Parent";

const Family = () => {
  // console.log(message);
  return (
    <div className="family">
      <Parent />
    </div>
  );
};

export default Family;
